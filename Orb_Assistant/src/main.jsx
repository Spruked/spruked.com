import React from 'react';
import ReactDOM from 'react-dom/client';
import FloatingOrb from './components/FloatingOrb';
import './components/FloatingOrb.css';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <React.StrictMode>
    <FloatingOrb />
  </React.StrictMode>
);