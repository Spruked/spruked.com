# CALI SKG v4.0 — Python Dependencies
# Install: pip install -r requirements.txt

# Core (always required)
# (No external core deps — uses stdlib + built-in sqlite3)

# Optional: CPU-optimized numerical computing
numpy>=1.24.0

# Optional: Deep learning backend (CPU/GPU)
# torch>=2.0.0+cpu  # Use CPU wheel if no GPU
# For GPU: torch>=2.0.0

# Optional: Knowledge graph
networkx>=3.0

# Optional: Sentence embeddings (falls back to deterministic hash encoder)
sentence-transformers>=2.2.0

# Optional: Voice / TTS (production)
# kokoro>=0.9.0
# faster-whisper>=1.0.0

# Optional: HTTP client for swarm APIs
# requests>=2.28.0
# aiohttp>=3.8.0

# Optional: Advanced diagnostics
# psutil>=5.9.0
