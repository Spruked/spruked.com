# CLI Commands

## apply
Apply vault logic to input.

```bash
vault apply --input "query" --vault "my_vault"
```

## trace
Show glyph traces.

```bash
vault trace --input "query"
```

## memory
Query memory.

```bash
vault memory --key "history"
```